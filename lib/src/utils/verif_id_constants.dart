/// Centralized spacing, radius, and sizing constants for the verif_id KYC screens.
abstract final class VerifIdConstants {
  // ── Spacing ──
  static const double pagePadding = 20.0;
  static const double sectionGap = 16.0;
  static const double itemGap = 12.0;
  static const double tinyGap = 8.0;

  // ── Radius ──
  static const double cardRadius = 18.0;
  static const double imageRadius = 14.0;
  static const double buttonRadius = 14.0;
  static const double badgeRadius = 20.0;

  // ── Capture Button ──
  static const double captureOuterSize = 72.0;
  static const double captureInnerSize = 56.0;
  static const double captureBorderWidth = 3.0;

  // ── Overlay ──
  static const double overlayDimOpacity = 0.45;
  static const double ovalWidthFactor = 0.58;
  static const double ovalHeightFactor = 0.68;
  static const double ovalCenterYFactor = 0.45;
  static const double bracketLength = 28.0;
  static const double bracketStroke = 3.0;
  static const double bracketCornerRadius = 8.0;
  static const double docWidthFactor = 0.82;
  static const double docHeightFactor = 0.48;

  // ── Icon Badge ──
  static const double iconBadgeSize = 40.0;
  static const double iconBadgeSizeLg = 80.0;

  // ── Button Heights ──
  static const double buttonHeight = 52.0;

  // ── Recording Indicator ──
  static const double recordingDotSize = 10.0;
}
